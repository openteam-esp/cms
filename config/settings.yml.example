production:
  hoptoad:
    api_key:            key
    host:               host

  s3:
    access_key_id:      key
    secret_access_key:  secret

news:
  protocol:             http
  host:                 localhost
  port:                 3000

appeals:
  protocol:             http
  host:                 localhost
  port:                 3001
