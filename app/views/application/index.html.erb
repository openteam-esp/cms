<% content_for :actions do %>
  <%= link_to action_title(:action => 'new'), new_resource_path %>
<% end %>

<%= content_tag :ul, :class => "collection_list" do %>
  <% collection.each do |site| %>
    <%= content_tag :li, link_to(site, site) %>
  <% end %>
<% end if collection.any? %>
