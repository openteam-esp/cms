page{
  link resource.title, resource.route
  template resource.template
  regions {
    resource.template_regions.keys.each do |reg|
      insert_value(reg, '', resource.part_for(reg).to_json)
    end
  }
}
