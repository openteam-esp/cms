page{
  link resource.title, resource.route
  template resource.template.slug
  regions {
    resource.template.regions.each do |reg|
      insert_value(reg.slug, '', resource.content_for(reg))
    end
  }
}
