page{
  link resource.title, resource.route
  template resource.template
  regions {
    resource.required_regions.each do |region|
      insert_value(region, '', resource.part_for(region).to_json)
    end
  }
}
