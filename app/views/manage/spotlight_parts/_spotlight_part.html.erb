<h3><%= spotlight_part.title %></h3>

<ol>
  <% spotlight_part.spotlight_items.each do |item| %>
    <li>
      <div class='presentation'>
        <b>№: <%= item.position %></b>
        <%= link_to item.url, item.url, :target => '_blank', :class => 'spotlight_url' %>
      </div>
      <hr />
    </li>
  <% end %>
</ol>
