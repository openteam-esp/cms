<%= render :partial => 'manage/news_list_parts/fields', :locals => { :form => form } %>

<%= form.inputs do %>
  <%= form.input :news_event_entry,
                 :as => :select,
                 :collection => form.object.class.values_for_select_tag(:news_event_entry),
                 :required => false %>
<% end %>

