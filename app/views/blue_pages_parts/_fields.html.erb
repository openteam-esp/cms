<%= form.inputs do %>
  <%= form.input :blue_pages_category_id, :as => :select, :collection => form.object.categories_options_for_select %>
  <%= form.input :region, :as => :hidden %>
  <%= form.input :type, :as => :hidden %>
<% end %>
